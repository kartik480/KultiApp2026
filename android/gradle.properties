# Use 2GB heap to avoid JVM/daemon crashes on memory-constrained systems
org.gradle.jvmargs=-Xmx2G -Dfile.encoding=UTF-8
android.useAndroidX=true
android.enableJetifier=true
# Build without daemon if you still see "daemon has disappeared" (slower but more stable)
# org.gradle.daemon=false
